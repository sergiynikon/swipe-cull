# Screenshots

The main README references two files in this folder:

- `delete.jpg` — swipe-left in progress, with the DELETE badge visible
- `keep.jpg` — swipe-right in progress, with the KEEP badge and the next card peeking underneath

Save the screenshots you took from the iPhone into this folder with those exact filenames so the README renders them on GitHub.
